<div class="media-slider">
  {% if props.has_label %}
    <div class="media-slider__label">{{ props.label }}</div>
  {% endif %}
  <div class="media-slider__inner">
    {{ props.media }}
  </div>
</div>
