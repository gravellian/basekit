<div class="image-slider">
  {% if props.has_label %}
    <div class="image-slider__label">{{ props.label }}</div>
  {% endif %}
  <div class="image-slider__inner">
    {{ props.images }}
  </div>
</div>

