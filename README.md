basekit theme, generated from starterkit_theme. Additional information on generating themes can be found in the [Starterkit documentation](https://www.drupal.org/docs/core-modules-and-themes/core-themes/starterkit-theme).

## Editor setup

Open the theme folder in VS Code so the bundled `.vscode/settings.json` takes effect. Install the Prettier extension (`esbenp.prettier-vscode`) and Twig/YAML support if prompted—saving SCSS, CSS, Twig, YAML, and JSON files will then format automatically to the Drupal-friendly Prettier rules defined in `.prettierrc`.
