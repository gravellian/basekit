{#
  Override for the article_index view.
  The view uses the "Rendered entity" row style, so row.content contains the
  complete article teaser rather than an array of individual Views fields.
#}
{{ attach_library('basekit/article_index_item') }}

{% for row in rows %}
  {{- row.content -}}
{% endfor %}
